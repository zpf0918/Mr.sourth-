<tr>
  <td>
    <%= product.id %>
  </td>
  <td>
    <%= render_product_status(product) %>
  </td>

  <td>
    <%= link_to product_path(product) do %>
       <% if product.photos.present? %>
        <%= image_tag(product.photos[0].avatar.thumb.url, class: "thumbnail") %>
      <% else %>
        <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
      <% end %>
    <% end %>
  </td>
  <td>
    <%= product.title %>
  </td>
  <td>
    <%= product.price %>
  </td>
  <td>
    <div class="order-btn">
      <%= link_to("编辑", edit_admin_product_path(product)) %>
    </div>
    <div>
      <%= link_to("向上", admin_product_path(product, status: "move_up"), method: :patch, :class => "btn btn-xs btn-info order-btn")%>
      <%= link_to("向下", admin_product_path(product, status: "move_down"), method: :patch, :class => "btn btn-xs btn-info order-btn")%>
    </div>
    <!--管理员下架商品-->
    <div>
    <% if product.is_hidden %>
     <%= link_to("上架", admin_product_path(product, status: "publish") , :method => :patch, :class => "btn btn-xs btn-success order-btn") %>
    <% else %>
     <%= link_to("下架", admin_product_path(product, status: "hide"), :method => :patch, :class => "btn btn-xs btn-danger order-btn") %>
    <% end %>
  </div>
  </td>

</tr>
